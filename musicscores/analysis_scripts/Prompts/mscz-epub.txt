When you initiate the command, the script will:
1. Target Lock: Read all filenames waiting in SVG_Staging (e.g., OBADIAH-001-1.svg, OBADIAH-001-2.svg).
2. Surgical Purge: Open the EPUB archive, isolate the specific folder wrapper for OBADIAH-001, and delete only the old SVG assets for that specific text block to ensure no phantom legacy pages are left behind.
3. Stream Injection: Copy the new SVGs out of staging and write them directly into the EPUB container.
4. Housekeeping: Safely delete the processed files from SVG_Staging so the inbox resets to clean for your next musicology session.
5. Staging Execution: MuseScore natively exports SVGs directly into the source directory it opened from. The Python update routine must scan this working source folder for renamed SVGs, execute the chapter-by-chapter EPUB replacement cycle, and safely delete the staging SVGs from the master directory once injection is complete.
6. Production System Directory Mapping:
   - Source/Staging Workspace: C:\Users\Bob\OneDrive\Documents\GitHub\SimHebrew-Concordance\musicscores and its subdirectories by volume. (Contains master .mscz, .xml, and local exported/renamed .svg files).
   - Target Ebook Environment: C:\Users\Bob\Calibre Library\D. Robert MacDonald. (Contains the master working .epub books grouped in subfolders by title).
   - Core Processing Logic: Script must crawl the Source Tree for active .svg files, map them by filename attributes to the corresponding target Calibre .epub container, execute a surgical chapter asset replacement, rewrite the epub safely to Calibre, and clear the source directory of the processed .svg strings.

Crawl through your GitHub subdirectories to sweep up any newly renamed .svg files.
Parse the filenames to instantly identify the Book and Chapter.
For the epubs in the KINDLE, unzipping the exact matching volume's .epub.
Perform the clean purge and injection of the fresh SVGs into the EPUB's internal asset tree.
Save the compiled EPUB straight back into Calibre, and safely delete the staging SVGs from your GitHub workspace to keep your repository clean.